apiVersion: v1
kind: Service
metadata:
  name: link-service
spec:
  selector:
    app: link-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
    - name: grpc
      protocol: TCP
      port: 50051
      targetPort: 50051