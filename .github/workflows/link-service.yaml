name: LinkService

on:
  push:
    branches: [ main ]
    paths:
      - "src/link-service/**"
      - "k8s/link-service/**"
      - ".github/workflows/link-service.yaml"
  workflow_dispatch:

jobs:
  run-pipeline:
    uses: ./service-template.yaml
    with:
      service-name: link-service
    secrets: inherit