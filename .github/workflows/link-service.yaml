name: LinkService

on:
  push:
    branches: [ main ]
    paths:
      - "src/link-service/**"
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build & Unit Tests
    runs-on: ubuntu-latest
    steps:
      # Clone repository to VM
      - name: Checkout
        uses: actions/checkout@v4

      # Install .NET to VM
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # Restore dependencies
      - name: Restore
        run: dotnet restore src/link-service/LinkService.csproj

  docker-publish:
    name: Build & Push Docker Image
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      # Clone repository to VM
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/link-service
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short
            type=ref,event=pr

      - name: Build & Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/link-service/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # deploy:
  #   needs: docker-publish
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: K3s deploy
  #       uses: azure/k8s-deploy@v5
  #       with:
  #         action: deploy
  #         manifests: |
  #           k8s/link-service/deployment.yaml
  #         images: |
  #           ghcr.io/${{ github.repository }}/link-service:sha-${{ steps.vars.outputs.sha_short }}
  #         kubectl-version: 'latest'