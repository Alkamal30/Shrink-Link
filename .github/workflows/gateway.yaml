name: Gateway

on:
  push:
    branches: [ main ]
    paths:
      - "src/gateway/**"
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build & Unit Tests
    runs-on: ubuntu-latest
    steps:
      # Clone repository to VM
      - name: Checkout
        uses: actions/checkout@v4

      # Install .NET to VM
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # Restore dependencies
      - name: Restore
        run: dotnet restore src/gateway/Gateway.csproj

  docker-publish:
    name: Build & Push Docker Image
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    outputs:
      tag: ${{ steps.meta.outputs.version }}
      repo: ${{ steps.lowercase.outputs.repo }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lower-case repo name
        id: lowercase
        run: echo "repo=${GITHUB_REPOSITORY,,}" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.lowercase.outputs.repo }}/gateway
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short,prefix=sha-
            type=ref,event=pr

      - name: Build & Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/gateway/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    name: Deploy to K3S
    needs: docker-publish
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'latest'

      - name: Set Kubernetes Context
        uses: azure/k8s-set-context@v4
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: K3s deploy
        uses: azure/k8s-deploy@v5
        with:
          action: deploy
          manifests: |
            k8s/gateway/deployment.yaml
          images: |
            ghcr.io/${{ needs.docker-publish.outputs.repo }}/gateway:${{ needs.docker-publish.outputs.tag }}